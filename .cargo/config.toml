[alias]
# Monorepo root: the build/check/run harness is the standalone xtask crate at
# xtask/ (NOT a workspace member). `cargo xtask <cmd>` routes here.
xtask = "run --manifest-path xtask/Cargo.toml --"